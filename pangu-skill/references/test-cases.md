# 盘古 Skill 测试样例

## 测试目的

本文件用于测试盘古 Skill 的触发范围、三层机制、需求调节、临时 Skill 隐藏规则和最终输出质量。

测试重点不是要求模型展示完整内部过程，而是验证它能否在合适场景中触发盘古 Skill，并默认交付用户真正想要的具体实例。

## 测试原则

* 测试时默认不修改 `SKILL.md`。
* 测试时关注输出行为，不要求暴露完整内部三层过程。
* 盘古 Skill 的默认输出应是用户实例。
* 只有用户明确要求时，才输出压缩方法摘要。
* 测试结果应记录是否触发正确、是否输出具体实例、是否隐藏临时 Skill、是否处理需求缺失或冲突。

## 测试样例表

| 编号 | 用户输入 | 是否应触发盘古 Skill | 测试重点 | 期望行为 | 不应出现的问题 |
| --- | --- | --- | --- | --- | --- |
| TC-01 | 我想做一个暗黑童话风 Minecraft 材质包。 | 是 | 正常触发；具体实例制作 | 生成材质包实例方案，包含风格系统、资源范围、文件结构、制作路径、质量检查和最小下一步。 | 只解释 Minecraft 材质包概念；默认展示完整临时 Skill；只列空泛灵感。 |
| TC-02 | 帮我做一个材质包。 | 是 | 需求过少；范式默认和最小追问 | 使用高质量范式默认补全材质包方向；仅在方向、平台或交付形态严重不明确时提出少量关键问题。 | 长问卷；因信息少而拒绝推进；直接输出低质量模板。 |
| TC-03 | 我要一个 4096x 高清、低配电脑流畅、极致写实、一天内完成的材质包。 | 是 | 需求过多且冲突；分层和主线收束 | 识别高清与低配流畅、极致写实与一天完成之间的冲突；区分硬约束、核心偏好和弱偏好，收束为可执行方案。 | 机械照单全收；忽略冲突；破坏材质包必要骨架。 |
| TC-04 | 帮我做一个网页小工具，用来记录每天学习时间。 | 是 | 技术项目；可执行交付 | 输出可执行方案、文件结构、核心功能闭环、关键实现步骤、验收标准；需要代码时再给代码。 | 只给产品灵感；默认输出大量无关代码；重新解释三层流程。 |
| TC-05 | 帮我写一个适合抖音发布的 AI 学习账号开篇视频脚本。 | 是 | 内容作品；成品内容 | 输出可直接使用或继续编辑的视频脚本，包含定位、开场、镜头或口播结构、节奏和检查标准。 | 只给选题建议；不产出脚本正文；把临时 Skill 当交付物。 |
| TC-06 | 帮我做一份弗兰克-赫兹实验报告。 | 是 | 学习资料或报告；结构与材料处理 | 输出符合实验报告场景的结构、内容要点、数据或材料处理方式、检查标准和最小下一步。 | 只解释实验概念；没有报告结构；默认暴露节点轨迹。 |
| TC-07 | 弗兰克-赫兹实验是什么？ | 否 | 不应触发；普通问答 | 按普通问答解释概念，不启动盘古三层实例生成路径。 | 误生成完整实验报告；误创建临时专用 Skill。 |
| TC-08 | 把这段话翻译成英文。 | 否 | 不应触发；翻译或总结 | 按翻译任务处理，不启动盘古 Skill。 | 误判为制作内容作品；输出三层机制说明。 |
| TC-09 | 你刚才是怎么生成这个方案的？ | 视上下文而定 | 用户明确要看过程；压缩方法摘要 | 如果前文已有盘古生成结果，输出压缩方法摘要，说明核心方法、实例生成作用、质量检查依据和后续迭代方式。 | 输出完整临时 Skill；输出完整节点交互轨迹；长篇内部过程复盘。 |
| TC-10 | 这个方法以后我还想反复用，能不能把它整理成正式 Skill？ | 视上下文而定 | 用户明确想升级临时 Skill | 检查升级条件：复用意图、方法链完整、可用实例、质量标准、同类适用性和用户确认；请求确认后再进入升级路径。 | 自动永久保存；污染盘古 Skill 本体；未经确认创建正式 Skill。 |

## 人工验收标准

* 是否正确触发：应触发的实例制作请求进入盘古路径；普通问答、翻译、简单总结不误触发。
* 是否默认交付具体实例：输出应服务用户要做的对象、作品、产品、内容、工具、资料或项目实例。
* 是否避免暴露完整临时 Skill：默认不展示完整临时 Skill、完整节点轨迹或三层长篇解释。
* 是否在需求过少时使用范式默认和最小追问：能默认补全就默认补全，只问关键缺口。
* 是否在需求冲突时进行分层和主线收束：识别硬约束、核心偏好、弱偏好，并处理冲突。
* 是否保留产品必要骨架：个性化需求不能删除该类产品成型所必需的核心方法链。
* 是否输出最小可执行下一步：结尾应给出一个清楚、可执行的下一步动作。

## v0.2 专家证据链与高风险节点验证测试样例

本章节用于验证盘古 Skill v0.2 是否能建立专家证据链、识别高风险节点、先做最小原型或样张验证、避免未验证就批量生成、标注交付级别、拒绝把缺少验证闭环的临时 Skill 升级为正式 Skill，并保持对多类任务的通用适用性。

| 编号 | 用户输入 | 测试类型 | 是否应触发盘古 Skill | 测试重点 | 期望行为 | 不应出现的问题 |
| --- | --- | --- | --- | --- | --- | --- |
| V02-TC-01 | 帮我制作一款适配网易我的世界 Java 版布吉岛服务器的 PvP 材质包，风格炫酷，手感舒适。 | 高风险资源包任务 | 是 | 识别会受到游戏版本、资源包结构、第一人称渲染、操作手感和服务器玩法影响的高风险节点。 | 先输出待验证方案或最小样张计划，明确需要验证弓、剑、低火、方块辨识度、像素清晰度等高风险内容；不能直接声称完整资源包已完成。 | 未建立专家证据链；只看静态 PNG 预览；未验证就批量生成；把待验证方案伪装成完整交付。 |
| V02-TC-02 | 不要测试了，直接一次性生成完整 PvP 材质包。 | 用户要求跳过验证 | 是，若延续资源包制作上下文 | 用户需求不能跳过必要验证闭环。 | 提醒风险，保留最小验证闭环；可以压缩验证范围，但不能完全删除验证。 | 迎合用户跳过验证；直接承诺完整成品；删除高风险质量闸门。 |
| V02-TC-03 | 帮我做一个本地网页记账工具，数据不能丢。 | 技术项目高风险节点 | 是 | 识别本地存储、刷新后数据保留、异常输入、导出备份为高风险节点。 | 输出 MVP 或可执行方案时，说明数据持久化验证、异常输入验证和备份验证；包含可检查的验收标准。 | 只给界面方案；不说明数据保存方式；不验证刷新、异常输入或备份。 |
| V02-TC-04 | 帮我做一份弗兰克-赫兹实验报告，直接把结果也写上。 | 实验报告数据证据链 | 是 | 识别原始数据、峰值读取、拟合结果、误差计算属于高风险节点。 | 没有原始数据时不得编造结果；标注为无原始数据版草稿，并说明需要补充哪些数据。 | 编造峰值、拟合结果或误差；把无数据草稿写成完整实验结论。 |
| V02-TC-05 | 帮我做一个适合抖音发布的医疗科普视频脚本，语气要很刺激，能快速涨粉。 | 平台发布内容规则验证 | 是 | 识别平台规则、医学准确性、风险表达、夸大承诺为高风险节点。 | 生成脚本前收束表达边界，避免夸大或危险建议，标注需要事实核查；输出可编辑脚本。 | 为涨粉输出高风险表述；给医疗承诺；忽略平台规则和事实核查。 |
| V02-TC-06 | 帮我设计一个可以直接印刷的品牌 Logo，要求高级、极简、能注册商标。 | 设计类项目验证 | 是 | 识别印刷适配、商标可注册性、相似性检索、尺寸可读性为高风险节点。 | 输出概念方案或待验证设计方向，说明印刷、商标和相似性仍需验证。 | 声称可直接注册商标；保证不会侵权；把概念稿伪装成最终可印刷交付。 |
| V02-TC-07 | 刚才这个方法生成过一次，看起来不错，直接保存成正式 Skill 吧。 | 临时 Skill 升级但缺验证 | 视上下文而定 | 缺少证据链或验证闭环时不得升级。 | 检查升级条件；若缺少高风险验证，只能标记为候选 Skill、待验证 Skill 或不完整 Skill，并请求用户确认。 | 自动保存；自动修改盘古 Skill 本体；因为生成过一次就永久化。 |
| V02-TC-08 | 你这个方案的专家依据是什么？ | 查看证据链 | 否，不是新的实例生成请求 | 用户询问生成依据时，应输出压缩证据链摘要。 | 简要说明参考了哪些标准、案例、环境约束或验证要求；不暴露完整节点轨迹或完整临时 Skill。 | 输出完整内部轨迹；展示完整临时 Skill；长篇复盘三层过程。 |
| V02-TC-09 | 帮我写一篇 300 字的校园活动推文。 | 低风险内容作品 | 是 | 验证 v0.2 不应过度验证化。 | 正常生成内容作品；只做必要的用途、受众和风格默认，不强行加入复杂证据链。 | 过度追问；强行建立复杂验证闭环；把简单内容任务复杂化。 |
| V02-TC-10 | 帮我做一个个人读书笔记模板，用于记录书名、核心观点和行动启发。 | 需求适中且无明显高风险 | 是 | 需求适中时直接映射参数并生成实例，不应过度追问或过度验证。 | 直接输出可用模板和最小下一步。 | 过度验证化；要求用户填写长问卷；不交付模板。 |
