# 盘古 Skill 项目工作规则

本仓库用于制作一个 instruction-only Codex/Agent Skill，名称暂定为“盘古 Skill”。

## 项目目标

盘古 Skill 的目标不是让用户制作 Skill，而是让用户说出想制作的对象后，系统内部通过三层机制生成用户真正想要的实例。

## 三层机制

1. 第一层：高质量范式实例生产层。
   - 生成领域造物专家。
   - 让专家在真实制作高质量普遍实例的过程中，只在关键制作节点上与 AI 交互。
   - 保留这些关键制作节点的交互轨迹。

2. 第二层：炼法成 Skill 层。
   - 把第一层的关键节点交互轨迹抽象成节点方法。
   - 将节点方法串联成方法链。
   - 将方法链封装为临时专用 Skill。

3. 第三层：用户实例生成层。
   - 调用临时专用 Skill，根据用户需求生成个性化实例。
   - 当用户需求过少时，使用范式默认和最小追问。
   - 当用户需求过多时，进行需求分层、冲突检测和主线收束。

## 临时 Skill 处理原则

临时 Skill 默认是内部中间产物，不默认展示给用户，也不默认永久保存。只有在经过验证且用户确认后，才可以升级为正式专用 Skill。

## 后续工作约束

- 后续工作必须一步一步进行。
- 每一轮只完成用户明确要求的当前阶段，完成后停止并等待确认。
- 禁止一次性生成完整项目。
- 禁止在没有确认的情况下重构整体架构。
- 第一版优先做纯文本 instruction-only Skill，不添加脚本、自动化程序或复杂依赖。
- 修改文件后，必须总结：改了哪些文件、为什么改、下一步建议是什么。
- 如果需求不清楚，先基于已有规格做最小合理推进，不编造不存在的功能。
